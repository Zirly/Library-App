<UserControl x:Class="Library.View.GenresListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="genreTemplate" >
            <TextBlock Text="{Binding Name}"/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ListBox ItemsSource="{Binding MyGenreList, Mode=TwoWay}" Name="lstGenres" HorizontalContentAlignment="Stretch" ItemTemplate="{StaticResource genreTemplate}" SelectionChanged="LstGenres_SelectionChanged"/>
    </Grid>
</UserControl>