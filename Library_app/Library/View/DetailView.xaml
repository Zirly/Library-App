<UserControl x:Class="Library.View.DetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View"
        xmlns:viewmodel="clr-namespace:Library.ViewModel"
        xmlns:view="clr-namespace:Library.View"
        mc:Ignorable="d"
        d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodel:BookDetailViewModel}">
            <view:BookDetail/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:AuthorDetailViewModel}">
            <view:AuthorDetailView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:GenreDetailViewModel}">
            <view:GenreDetailView/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <StackPanel x:Name="spDetail">
            <ContentControl x:Name="detailContent" Content="{Binding}"/>
        </StackPanel>
        <!--
        <StackPanel x:Name="authorDetailData" Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" Text="First Name:" />
                <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding FirstName}"/>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Last Name:" />
                <TextBox Grid.Column="1" Grid.Row="1"  Text="{Binding LastName}"/>
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Year of birth:" />
                <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding YearBirth}"/>
            </Grid>
            
            <TextBlock Text="Books:" />
            <TextBox Text="{Binding Description}" Margin="5,10,10,0" 
                     Width="350" Height="Auto"
                     HorizontalScrollBarVisibility="Visible"
                     TextWrapping="Wrap"
                     TextAlignment="Left"
                     MaxLength="500"
                     IsReadOnly="False"
                     AcceptsReturn="True" />
        </StackPanel> -->
    </Grid>
</UserControl>
