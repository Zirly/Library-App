<UserControl x:Class="Library.View.AuthorDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.View"
             xmlns:viewmodel="clr-namespace:Library.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" Text="Name:" />
                <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding MyAuthor.FullName, Mode=OneWay}"/>
                <TextBlock Grid.Column="0" Grid.Row="1" Text="Year of birth:" />
                <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding MyAuthor.YearBirth}"/>
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Books:" />
                <ListBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding MyAuthor.BooksList}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
