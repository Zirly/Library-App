<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="Library.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- MENU -->
            <RowDefinition Height="*"/>
            <!-- VIEW -->
            <RowDefinition Height="Auto"/>
            <!-- BUTTONS -->
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <System:String>File Options</System:String>
        </Menu>
        <Grid Grid.Row="1" Margin="10,0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ListBox Grid.Column="0" Margin="0">
                <System:String>COLLECTION</System:String>
                <ListBox x:Name="lstTest" Height="100" Width="100"/>

            </ListBox>
            <ListBox Grid.Column="1" Margin="0">
                <System:String>DETAILS</System:String>
            </ListBox>
        </Grid>
        <StatusBar Grid.Row="2" Height="40" >
            <Button Width="55" Margin="20,0,0,0" Content="Add Item" />
            <Button Width="55" Margin="20,0,0,0" Click="TestDB_Click" Content="Test"/>
            <Button HorizontalAlignment="Right" Margin="580,0,0,0" Width="30" Content="Save"/>
            <Button Height="20" Width="40" Margin="20,0" Content="Cancel"/>
        </StatusBar>
    </Grid>
</Window>
