<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        xmlns:view="clr-namespace:Library.View"
        xmlns:viewmodel="clr-namespace:Library.ViewModel"
        xmlns:System="clr-namespace:System;assembly=mscorlib" x:Class="Library.MainWindow"
        mc:Ignorable="d"
        Title="Library Application" Height="450" Width="800" Activated="Window_Activated" >
    <Window.Resources>
        <DataTemplate x:Name="booksListTemplate" DataType="{x:Type viewmodel:BooksListViewModel}">
            <view:BooksListView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="authorsListTemplate" DataType="{x:Type viewmodel:AuthorsListViewModel}">
            <view:AuthorsListView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="genresListTemplate" DataType="{x:Type viewmodel:GenresListViewModel}">
            <view:GenresListView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="genreDetailTemplate" DataType="{x:Type viewmodel:GenreDetailViewModel}">
            <view:GenreDetailView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="authorDetailTemplate" DataType="{x:Type viewmodel:AuthorDetailViewModel}">
            <view:AuthorDetailView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="bookDetailTemplate" DataType="{x:Type viewmodel:BookDetailViewModel}">
            <view:BookDetail DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
    <Grid x:Name="myGrid" Background="#f1eded">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- MENU -->
            <RowDefinition Height="40" />
            <RowDefinition Height="Auto"/>

            <!-- SEARCH -->
            <RowDefinition Height="4*"/>
            <!-- VIEW -->
            <RowDefinition Height="Auto"/>
            <!-- BUTTONS -->
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"/>              
                <MenuItem Header="_Save"/>
                <Separator/>
                <MenuItem Header="_Exit"/>
            </MenuItem>
            <MenuItem Header="_Options"/>
        </Menu>
        <Border Grid.Row="1" Margin="10,0" BorderThickness="0,0,0,0" BorderBrush="AliceBlue">
        <Grid Grid.Row="1" Background="Coral">
            <TextBlock Width="Auto" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0">Show:</TextBlock>
            <ComboBox x:Name="comboBox" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="44,10,0,0" Width="70">
                <ComboBoxItem Selected="BooksSelection_Selected" IsSelected="True" >Books</ComboBoxItem>
                <ComboBoxItem Selected="AuthorsSelection_Selected" >Authors</ComboBoxItem>
                <ComboBoxItem Selected="GenresSelection_Selected">Genre</ComboBoxItem>
            </ComboBox>
            <TextBlock Width="Auto" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="140,10,0,0">Search</TextBlock>
        </Grid>
        </Border>
        <Grid Margin="10,0" Grid.Row="2" Background="Coral">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="COLLECTION" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="20,0,0,0" FontWeight="Bold" FontFamily="Arial"/>
            <TextBlock Grid.Column="1" Text="DETAILS" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="7,0,0,0" FontWeight="Bold" FontFamily="Arial"/>
        </Grid>
        <Grid Grid.Row="3" Margin="10,0" Background="AliceBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="lstBase" Grid.Column="0" Margin="0" Background="AliceBlue">
                <ContentControl Content="{Binding collection}" Background="AliceBlue"/>

                <!--<view:BooksListView x:Name="BooksListControl"/> -->
            </Grid>
            <ListBox Grid.Column="1" Margin="0" Background="AliceBlue">
                <ContentControl Content="{Binding detail}"/>
            </ListBox>
        </Grid>
        <StatusBar Grid.Row="4" Height="40" >
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False" />
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem DockPanel.Dock="Left" Margin="10,0">
                <Button Width="55"  Content="Add Item" Click="Item_Add_Button_Click"/>
            </StatusBarItem>
            <StatusBarItem DockPanel.Dock="Left"/>
            <StatusBarItem  DockPanel.Dock="Right" Margin="10,0">
                <Button Width="55" Content="Exit" Click="Exit_Button_Click"/>
            </StatusBarItem>
            <StatusBarItem DockPanel.Dock="Right" >
                <Button Width="55"  Content="Save" Click="Save_Button_Click"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
